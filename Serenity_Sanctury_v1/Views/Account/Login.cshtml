@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "Login";
}
@using Serenity_Sanctury_v1.ViewModels
@model LoginViewModel
<body>
    <div class="container">
        <form asp-action="Login" class="login-form">
            <div class="mb-3 bg p-5 rounded" style="box-shadow: 2px 2px 3px 2px darkgray;">
                <h2 class="text-center mb-4 text-white">Login</h2>

                @* Display error message if present *@
                @if (TempData["Error"] != null)
                {
                    <div class="col-md-12 alert alert-danger">
                        <span><b>Sorry!</b> - @TempData["Error"]</span>
                    </div>
                }

                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group mt-4">
                    <label asp-for="Email" class="control-label fw-bold text-white"></label>
                    <input asp-for="Email" class="form-control" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>
                <div class="form-group mt-4">
                    <label asp-for="Password" class="control-label fw-bold text-white"></label>
                    <input asp-for="Password" class="form-control" />
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>
                <div class="form-group d-flex justify-content-center mt-3">
                    <input class="btn-login float-right w-50 mt-3 fw-bold rounded border" type="submit" value="Log in" />
                    <a class="btn btn-outline-secondary ms-4 mt-3 w-50 fw-bold text-white border" asp-controller="Products" asp-action="Index">Cancel</a>
                </div>
                <div class="form-group mt-4 d-flex justify-content-center">
                    <a class="text-white" asp-area="" asp-controller="Account" asp-action="ForgotPassword">Forgot Password?</a>
                </div>
                <div class="form-group mt-4 d-flex justify-content-center">
                    <p class="text-center text-white">Don't have an account? --></p>
                    <a class="text-white fw-bold ms-1" asp-area="" asp-controller="Account" asp-action="Register">Create</a>
                </div>
            </div> 
        </form>
    </div>
</body>

